// Export all traffic-related components
export { default as TrafficChart } from './TrafficChart';
export { default as TrafficWidget } from './TrafficWidget';

// Re-export types if needed
export type { TrafficSample, TrafficResponse, InterfaceData } from '@/types/traffic';
